databases:
  # 1. The PostgreSQL Database
  - name: hiremap-db
    plan: free
    databaseName: hiremap
    user: hiremap_user

services:
  # 2. Redis Cache
  - type: redis
    name: hiremap-redis
    plan: free
    ipAllowList: [] # empty means all services in this Render account can connect

  # 3. The Node.js Web App
  - type: web
    name: hiremap-api
    env: node
    plan: free
    region: singapore # Change to your preferred region (e.g., frankfurt, oregon)
    buildCommand: "cd backend && npm ci && npm run build && npx prisma generate && npx prisma migrate deploy"
    startCommand: "cd backend && npm run start"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hiremap-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: hiremap-redis
          property: connectionString
      - key: NODE_ENV
        value: production
      - key: ENABLE_CRON
        value: "true"
      # You MUST manually fill these out in the Render Dashboard after importing:
      - key: JWT_SECRET
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
